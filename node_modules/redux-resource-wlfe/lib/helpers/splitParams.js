'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = splitParams;

var _includes = require('./includes');

var _includes2 = _interopRequireDefault(_includes);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function splitParams(params, pathParamKeys) {
  var pathParams = {};
  var queryParams = [];

  Object.keys(params).forEach(function (paramKey) {
    if ((0, _includes2.default)(pathParamKeys, paramKey)) {
      pathParams[paramKey] = params[paramKey];
    } else {
      queryParams.push(paramKey + '=' + encodeURIComponent(params[paramKey]));
    }
  });

  return [pathParams, queryParams.join('&')];
}